model User {
    id       Int      @id @default(autoincrement())
    name     String
    email    String   @unique
    password String
    position String?
    createAt DateTime @default(now())
    updateAt DateTime @default(now())
}
